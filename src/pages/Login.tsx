import React from "react";
import LoginForm from "components/LoginForm";

const Login: React.FC = () => (
  <main className="flex items-center h-screen justify-center bg-gray-50 bg-chat-pattern bg-cover bg-center">
    <LoginForm />
  </main>
);

export default Login;
